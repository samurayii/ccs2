{
    "type": "object",
    "properties": {
        "session_token": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
        },
        "pattern": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256,
            "pattern": "^(/|)[a-zA-Z0-9]{1}.{0,255}"
        }
    },
    "additionalProperties": false,
    "required": [
        "session_token",
        "pattern"
    ]
}